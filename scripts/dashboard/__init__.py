"""BSA dashboard package.

Operator-side static-HTML dashboard generator (v1.3.3 — WIP).
Read-only viewer over canonical artifacts + handoff packets + audit
reports + sidecar diagrams + Phase 7 telemetry.

Entry point: scripts/generate_dashboard.py.
"""
